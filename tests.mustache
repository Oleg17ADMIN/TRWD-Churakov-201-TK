<!DOCTYPE html>
<html lang="uk">
  <head>
    <meta charset="UTF-8">
    <title>{{title}}</title>
    <link rel="stylesheet" href="/css/style.css">
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;600;700&display=swap" rel="stylesheet">
  </head>
  <body>
    {{> header}}
    
    <main class="container mt-4">
      <div class="hero">
        <h1>{{title}}</h1>
        <p class="hero-description">Перегляньте наявні тести або додайте новий тест.</p>
        {{#user}}
          <div class="hero-buttons">
            <a href="/tests/new" class="btn btn-primary">Створити новий тест</a>
          </div>
        {{/user}}
      </div>
      
      <section class="projects">
        <h2 class="section-title blue">Список тестів</h2>
        <div class="projects-grid">
          {{#tests}}
            <div class="project">
              <img src="/images/test_placeholder.jpg" alt="Test Image">
              <div class="project-link">
                <span class="project-title">Тест: </span>
                <span class="project-name">{{title}}</span>
              </div>
              <div class="project-actions mt-2">
                <a href="/tests/{{_id}}" class="btn btn-primary">Переглянути</a>
                {{#user}}
                  <a href="/tests/{{_id}}/edit" class="btn btn-outline">Редагувати</a>
                  <form action="/tests/{{_id}}?_method=DELETE" method="POST" style="display:inline;">
                    <button type="submit" class="btn btn-danger">Видалити</button>
                  </form>
                {{/user}}
              </div>
            </div>
          {{/tests}}
        </div>
        {{^tests}}
          <p class="alert alert-info text-center">Тестів поки немає.</p>
        {{/tests}}
      </section>
    </main>
    
    {{> footer}}
    
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
  </body>
</html>